中文预定！

现在在等着吃午饭，为什么要等呢，因为他就在那，可是我不饿，我如果现在吃了我晚上肯定会很饿，所以我得忍住不吃，所以我就给自己找了这件事情，没错，这件事情就是，就是，就是，就是，就是，就是，就是！！！！写个板子，设计板子，有设计内容的板子，夹了个设计稿的板子，对设计稿，这件事情就是写个这个东西

设计什么呢？一个便签程序，LC独创的便签程序，他能干嘛呢？它能快速唤醒和隐退，唤醒时可以快速查找到之前写的便签，还可以输入密语进入仿真桌面，所以为了实现这么个效果，我们就需要确定一个方法，它能够让便签程序的唤醒和隐退称的上快速（mark1），让后得有个搜索方法，这个方法能在众多内容中找出较为匹配的所有便签并且支持按tab轮询，最好在打开后留一个快速退回打开之前那一刻状态的功能，因为有可能开错了，这可以省掉很多因为开到近似记录而重新查找的时间，为了实现这个，首先我们需要一个便签集合，就是记录便签记录的一个地方（mark2），然后定义一个搜寻方式（mark3），输出最匹配记录集合，最好给个匹配度调节阀，如果可以更可以给个切换匹配引擎的切换口，这里就牵扯了一系列显示方法的实现（mark4），从mark1到4依次是：便签程序入退的位置在屏幕右下角，暂定闪现闪退，全局快捷控制，好，mark1结束，mark2的话暂定使用postgreSQL数据库，mark3暂定是通过程序顶部的一个文本输入框获取匹配参照字符串，mark4的话就是就是这里的内容了，不太严谨，剩下的才是mark4内容，即来自另一个文件的描述：
常驻后台，全局快捷键呼出便签悬停板，此时焦点在便签搜索栏，按tab（shift tab）可以轮选最近打开的几个便签，搜索栏会在搜索到相应便签时在悬停板上方显示便签预览浮板，浮板只做预览效果，无法聚焦，在搜索栏按enter随即打开便签编辑窗口，即markdown编辑器，焦点在最近便签时按下enter同样打开编辑器，编辑对应的便签。如果连按三下全局快捷键（按两下是关闭）则会打开便签桌面，在此桌面上每一个便签都对应一张纸，双击会打开编辑器，同时可以拖动纸张，拖动的纸张总在最顶层，但是如果原先在一张纸下面从拖动开始到拖动停止时都一直保持对一张纸的重叠且重叠时拖动的纸在非拖动纸之下，此时仍然保留原先的高低顺序，包括比非拖动纸高的也保留原先的顺序

所以我们第一步要做的就是：
把主窗口搞出来，绑定全局快捷键，显示位置
主窗口名叫mptFloatPlane，mpt是markPaperAndTable的缩写
    {UI美化，显隐逻辑，启动时就算点其他地方还是不会转到透明，其他情况下转到透明又太快了，后期或许加个动画，然后界面太老套，以后换成现代简洁暖色风}
第二步：
定义预览窗口并插入主窗口，试探与postgreSQL的交互，把项目上传到公共平台
    {多个preview的排版以及浏览方法}
第三步：
抽象出笔记与视图，即让笔记内容与UI解耦
    {笔记内容类的功能有待完善}
》第四步：
初始化搜索引擎；搜索结果悬浮窗添加，或许可以和之前的preview共用，只不过在搜索中先悬浮一个“搜索中”提示
 4.1 统一化预览和搜索展示
 4.2 完善文档收纳处理系统
 4.3 待补充
第五步：
整体布局和UI调优
第六步：
等上一步完成



mark一个点子：
想要利用我自己的，不知道别人有没有的一种能力，就是假如有一张纸，有的时候你不太记得第几行，但是你大概记得在一张纸的哪个位置，那我们就叫人脑对位置信息敏感的特点吧，所以计划实现一个这样的功能，就是每个预览窗都是一个缩放平铺的纸张，点击预览窗口后会给出对应纸张位置的内容，嗯嗯嗯嗯嗯嗯嗯嗯，不抽象吧，感觉描述完了，先这样就就就了
还记得小时候写作文的插入抹除吗？实现它！再加一个手动的整合按钮，相信我，这是划时代的！
加一个手动的最小化按钮，但是最小化后保留按钮，且按钮可移动，主要目的是为了不使失焦时妨碍对其他应用的操作
